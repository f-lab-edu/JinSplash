import { styled } from 'styled-components'

export default function LogoComponent() {
	return <Logo>Logo svg자리</Logo>
}

const Logo = styled.div`
	color: blue;
	align-items: center;
	display: flex;
`
